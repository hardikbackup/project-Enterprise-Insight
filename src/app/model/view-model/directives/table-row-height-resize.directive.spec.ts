import { TableRowHeightResizeDirective } from './table-row-height-resize.directive';

describe('TableRowHeightResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new TableRowHeightResizeDirective();
    expect(directive).toBeTruthy();
  });
});
