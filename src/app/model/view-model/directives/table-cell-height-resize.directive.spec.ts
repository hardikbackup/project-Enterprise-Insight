import { TableCellHeightResizeDirective } from './table-cell-height-resize.directive';

describe('TableCellHeightResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new TableCellHeightResizeDirective();
    expect(directive).toBeTruthy();
  });
});
