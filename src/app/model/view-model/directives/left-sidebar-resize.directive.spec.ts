import { LeftSidebarResizeDirective } from './left-sidebar-resize.directive';

describe('LeftSidebarResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new LeftSidebarResizeDirective();
    expect(directive).toBeTruthy();
  });
});
