import { TableColumnResizeDirective } from './table-column-resize.directive';

describe('TableColumnResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new TableColumnResizeDirective();
    expect(directive).toBeTruthy();
  });
});
